version: "3.8"

services:
  mysql:
    image: mysql:8.0
    container_name: userprofileervice-mysql
    environment:
      MYSQL_ROOT_PASSWORD: 1111
      MYSQL_DATABASE: userprofileervice
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
